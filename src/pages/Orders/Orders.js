import { PageContainer, PageHeader } from "../../components";

const Orders = () => {
  return (
    <PageContainer>
      <PageHeader title="Encomendas" />
    </PageContainer>
  );
};

export default Orders;
