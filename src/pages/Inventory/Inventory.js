import { PageContainer, PageHeader } from "../../components";

const Inventory = () => {
  return (
    <PageContainer>
      <PageHeader title="Inventário" />
    </PageContainer>
  );
};

export default Inventory;
